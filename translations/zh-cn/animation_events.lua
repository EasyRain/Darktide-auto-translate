-- Auto Translate seed translations for mod: animation_events  (language: zh-cn)
-- manual = true: hand written translations; machine translation never overwrites them.
-- Set enabled = false to skip this mod entirely.
return {
    enabled = true,
    manual = true,
    entries = {
        ["mod_description"] = { text = "动画事件（第三方 mod 未提供描述）。" },
    },
}
