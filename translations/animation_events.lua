-- Auto Translate seed translations for mod: animation_events
-- Entries with src = "manual" are never overwritten by machine translation.
-- Set enabled = false to skip this mod entirely.
return {
    enabled = true,
    entries = {
        ["mod_description"] = { zh = "动画事件（第三方 mod 未提供描述）。", src = "manual" },
    },
}
